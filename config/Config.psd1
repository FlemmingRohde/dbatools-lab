@{
    # This should be the folder you have extracted the install media to and there should be two folders in here '2017' and '2019'
    InstallMediaPath = 'C:\Install'

    # This will be used to download the WideWorldImporters backup to, and can be used as a backup destination for the labs
    BackupPath = 'C:\dbatoolslab\Backup'

    # This will be used to download the WideWorldImporters backup to, and can be used as a backup destination for the labs
    ComputerName = 'DBATOOLS-DEV-0'

    SqlInstance = "SQL2019"

    SqlNamedInstance = "SQL2017"
}